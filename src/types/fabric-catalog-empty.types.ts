/** Copy for the catalog zero-results view (mirrors root `catalogNoResults` in messages). */
export interface FabricCatalogNoResultsCopy {
  title: string
  description: string
  popularHeading: string
  ctaSourcing: string
  ctaContact: string
}
