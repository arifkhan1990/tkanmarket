/**
 * Authenticated admin `<main>` inner padding — matches `/admin/dashboard` content inset.
 * Applied once in `AdminLayoutShell` so every dashboard route shares the same horizontal rhythm.
 */
/** Same horizontal inset as `<main>` — use on `fixed` bars that sit outside the padded content wrapper. */
export const ADMIN_MAIN_CONTENT_HORIZONTAL_PADDING_CLASS = 'px-4 sm:px-6 lg:px-8'

export const ADMIN_MAIN_CONTENT_PADDING_CLASS = 'px-4 py-6 sm:px-6 lg:px-8'
