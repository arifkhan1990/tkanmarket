/** Public path — used when a fabric has no `images` in DB. */
export const FABRIC_IMAGE_PLACEHOLDER_PATH = '/placeholder-fabric.svg'

/** Public path — used when a supplier has no logo / cover in DB. */
export const SUPPLIER_LOGO_PLACEHOLDER_PATH = '/placeholder-supplier.svg'
