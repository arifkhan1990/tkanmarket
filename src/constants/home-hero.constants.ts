/**
 * Optional YouTube video ID for the home hero “watch tour” dialog (privacy-enhanced embed).
 * When null, the play control links to the contact page instead.
 */
export const HOME_HERO_YOUTUBE_EMBED_ID: string | null = null
